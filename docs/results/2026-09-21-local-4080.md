# Локальная проверка B2W на RTX 4080 Laptop

Машина: Windows 11, RTX 4080 Laptop 12 GB, driver 616.92, Python 3.11.9. Isaac Sim 5.1.0 и PyTorch 2.7.0+cu128 используются из `D:\isaacsim51`; Isaac Lab v2.3.2 взят из официального tag archive в `.runtime/IsaacLab`. Это рабочий локальный runtime, но ещё не полностью независимая установка: бинарные пакеты Isaac Sim берутся из существующего окружения. Старый B2W проект и его jobs не использовались.

Снимок 226 пакетов Isaac Sim окружения, пакетов project venv, GPU/driver и SHA-256 локальных upstream archives: [runtime lock](2026-09-21-local-4080-runtime.json).

## Итог на 21 сентября 2026

- Локальный headless runtime на RTX 4080 Laptop прошёл этап 0; импортированный flat checkpoint seed 54 прошёл closed-loop проверку 128/128 эпизодов.
- Из общего flat actor обучены три rough политики с независимыми rough seeds 54–56. На удержанном level 9 каждая проверена на mesh seeds 2001–2003 (1536 эпизодов по 20 с): безопасно 1515/1536, 1523/1536 и 1524/1536 соответственно. Flat regression для каждого checkpoint прошла 128/128 эпизодов; tracking остался в пределах проектного допуска 10%.
- Дополнительное обучение seed 54 с reset ±0.3 рад (stage 2) ухудшило forward tracking; обучение с начальным доступом к level 9 (stage 3) сохранило общий результат 1515/1536, перераспределив ошибки между семействами. Эти абляции не заменили базовые rough checkpoints.
- Контрольный rough run с нуля остановлен по просьбе пользователя на обновлении 331/350 после падения среднего terrain level до 0; финального checkpoint и closed-loop оценки нет. Сейчас обучение не запущено.
- По текущей точечной оценке seed 56 — кандидат для следующей проверки, но выбор релизной политики не сделан: нужны новые геометрии/семейства лестниц, sim2sim и проверка контракта экспорта. Эти результаты не разрешают испытания на роботе.

## Этап 0

- Headless Isaac Sim: прошёл.
- Flat B2W, 16 сред, 2500 policy steps = 10 000 physics steps: прошёл за 83.2 с, без NaN и аварийного завершения. Лог: `logs/smoke_b2w_16_2500.log`.
- Rough B2W, 16 сред, 100 policy steps: прошёл. Лог: `logs/smoke_b2w_rough_16_100.log`.
- GPU PhysX работал на `cuda:0`; во время flat smoke через NVML наблюдалось около 3.1 GB видеопамяти. Предупреждения Kit о shader cache и Aftermath не остановили симуляцию.
- Официальный `default_environment.usd` Isaac 5.1 кеширован в `.cache/assets/ground`, SHA-256 `8a21c317d638d33a4e6c20c958a7ed8f4d8c5195efd7e20f1b85178b4a15638a`. Для headless отключены только сетевые визуальные ресурсы и debug arrows.

## Flat handoff seed 54

Пользователь указал архив `D:\Work\GitProjects\B2W_RL_IsaacLab\logs\transfer\b2w_flat54_handoff_20260921.tar.gz`. SHA-256 архива `064d01580b621a361cf83c708ad9d620a7d23aec98d09b364be1627c953defaf`. Все 23 payload-файла проверены по SHA-256 и размеру; копия находится в `.cache/h` текущего проекта. SHA-256 полного checkpoint `model_349.pt`: `3eeb00963e528f6b31607a0188691f123b9ba533288a58c931c98ef790cab4fc`. Vendor manifest внутри handoff совпал с текущим `vendor/manifest.json` (`2f0e2ea83aa9808dc10787f6dc34f6dd23ec7bb96c45acd4b747250ee72dfa1a`).

Handoff требует git commit `5add7c9de63ab44ae8e3f250f59e05f74452478d`; локальный checkout пока `b1dab208b63542b1d4fbcc7603be3cd6acf756f4`. Поэтому новые прогоны фиксируются как локальная проверка и адаптация, а не точное продолжение исходного run. Сам handoff сообщает о пройденной flat qualification для seeds 54–56, но его export report явно отмечает отсутствие прежней closed-loop проверки в Isaac на этом ПК.

Локальная closed-loop проверка экспортированного flat actor: 128 сред × 1000 policy steps = 20 секунд на среду. Все 128 сред дошли до тайм-лимита; досрочных завершений, низкой посадки ниже 0.25 м и сильного наклона не обнаружено. RMS ошибок `(vx, vy, yaw)` = `(0.161, 0.175, 0.150)` в единицах м/с, м/с, рад/с. Лог: `logs/eval_flat54_128_1000.log`. Это начальный тест штатного распределения команд, не held-out оценка rough/stairs.

## Rough warm start

Actor 57→16 перенесён из `model_349.pt`; rough critic имеет вход 247 вместо flat 60, поэтому critic и optimizer созданы заново. Exploration std оставлен 0.1; PPO: learning rate `1e-4`, fixed schedule, clip `0.1`, entropy coefficient `0`. Первый curriculum ограничивает reset roll/pitch до ±0.1 рад. Actor заморожен на первые 50 обновлений основного run, затем размораживается.

Пилоты на 256 и 1024 средах по 10 обновлений завершены с конечными весами и checkpoint. На 256 средах наблюдалось около 4.4 GB VRAM, на 1024 средах около 4.8 GB. Основной run: seed 54, 1024 среды, 350 обновлений за 751.14 с, `logs/train_b2w_rough_from_flat54_stage1.log`. Финальный checkpoint `logs/rsl_rl/unitree_b2w_rough/2026-09-21_11-29-39_rough_from_flat54_stage1/model_349.pt`, SHA-256 `916bf5c5b4e5ca43ecfacd4bde6c5a92b164b9c0a6d5c9257a6c7ed68662febf`. Все веса конечны; actor input 57, critic input 247.

### Первичная closed-loop оценка stage 1

128 сред × 1000 policy steps (20 с) на каждом новом seed, с тем же upright reset ±0.1 рад. Успех в этой проверке означает достижение тайм-лимита без досрочного завершения, наклона свыше примерно 60° или контакта `base_link`/четырёх hip links силой выше 5 Н. Штатное `illegal_contact` termination в rough выключено, поэтому контакты подсчитаны отдельно. Абсолютную высоту корпуса на rough не использовали как признак падения из-за разной высоты рельефа.

| Seed | Безопасно/всего | RMS vx, м/с | RMS vy, м/с | RMS yaw, рад/с |
|---|---:|---:|---:|---:|
| 1001 | 128/128 | 0.196 | 0.201 | 0.165 |
| 1002 | 128/128 | 0.193 | 0.203 | 0.164 |
| 1003 | 128/128 | 0.205 | 0.202 | 0.177 |

Логи: `logs/eval_rough_stage1_seed1001_contacts.log`, `logs/eval_rough_stage1_seed1002.log`, `logs/eval_rough_stage1_seed1003.log`. Это held-out seeds процедурного генератора, но ещё не отдельные семейства рельефа и не восстановление из переворота.

Flat regression на seed 1001: 128/128 безопасных эпизодов как у исходного flat actor. RMS rough checkpoint `(0.1629, 0.1726, 0.1461)` против исходного flat `(0.1633, 0.1702, 0.1457)`; относительное изменение `(-0.22%, +1.43%, +0.29%)`. Логи: `logs/eval_flat_regression_stage1_seed1001.log` и `logs/eval_flat_parent_seed1001.log`.

Эта первичная оценка ещё не разделяла семейства рельефа и не измеряла slip/torque saturation; дополнительные проверки приведены ниже. Stage 1 не является готовой политикой для лестниц или робота.

### Проверка расширения reset до ±0.3 рад

Из `model_349.pt` возобновлён полный checkpoint (actor, critic, optimizer) с тем же PPO-конфигом; симулятор и RNG инициализировались заново. Дополнительно выполнено 200 обновлений за 424.57 с; финальный `logs/rsl_rl/unitree_b2w_rough/2026-09-21_12-00-49_rough_tilt03_stage2/model_548.pt`, SHA-256 `d7460958e4652f327eb45e43ec78257994fd480ca555163ea52ae0ab9a33bdb8`. Лог `logs/train_b2w_rough_tilt03_stage2.log`. Веса конечны.

Оба checkpoint проверены на одинаковых rough seeds 1001–1003, по 128 сред × 20 с на seed, с reset roll/pitch ±0.3 рад. На всех 384 эпизодах у обоих checkpoint не обнаружены сильный наклон, контакт base/hip >5 Н или досрочное завершение.

| Checkpoint | Средний RMS vx | Средний RMS vy | Средний RMS yaw | Flat regression против flat parent |
|---|---:|---:|---:|---|
| Stage 1 `model_349.pt` | 0.1974 | 0.2054 | 0.1660 | `(-0.22%, +1.43%, +0.29%)` |
| Stage 2 `model_548.pt` | 0.2075 | 0.2052 | 0.1643 | `(+3.80%, +2.88%, -1.26%)` |

Stage 2 не дал выигрыша в безопасности, а forward tracking ухудшился на 5.1% относительно stage 1; yaw улучшился на 1.0%. Для следующей абляции родителем оставлен stage 1 `model_349.pt`. Stage 2 — отдельная абляция. Логи одинаковых rough сценариев: `logs/eval_rough_stage1_tilt03_seed100{1,2,3}.log` и `logs/eval_rough_stage2_tilt03_seed100{1,2,3}.log`; flat stage 2: `logs/eval_flat_regression_stage2_seed1001.log`.

Результаты этой абляции не распространяются на лестницы и live robot.

### Удержанный rough, фиксированный уровень 9

Основной stage 1 `model_349.pt` проверен на трёх новых seeds процедурного mesh 2001–2003: по 512 сред × 1000 policy steps (20 с), одинаковый диапазон начального roll/pitch ±0.3 рад. Curriculum во время проверки выключен, все среды поставлены на самую сложную строку генератора (level 9). Небезопасным считается хотя бы один сильный наклон, контакт base/hip >5 Н или досрочное завершение; `illegal_contact` в исходной rough задаче выключен, поэтому измерен явно. Абсолютная высота корпуса не входит в критерий на переменном рельефе.

| Seed | Безопасно/всего | RMS vx/vy/yaw | Обратные ступени: безопасно/всего | Средняя сумма `abs(torque × joint velocity)`, Вт/среду | Доля torque saturation | Горизонтальный slip proxy, м/с |
|---|---:|---|---:|---:|---:|---:|
| 2001 | 506/512 | 0.232/0.227/0.193 | 98/102 | 667.5 | 1.32% | 0.275 |
| 2002 | 505/512 | 0.254/0.234/0.201 | 96/102 | 697.3 | 1.36% | 0.301 |
| 2003 | 504/512 | 0.239/0.238/0.199 | 97/102 | 696.8 | 1.37% | 0.295 |

Всего 1515/1536 (98.63%) безопасных. Из 21 небезопасного эпизода 15 пришлись на `pyramid_stairs_inv`, 4 на `boxes`, 2 на `pyramid_stairs`; остальные три семейства дали 0. На seed 2002 `pyramid_stairs_inv` имеет 94.1% безопасности, ниже проектного порога 95% по семейству. Повтор seed 2002 с учётом времени событий воспроизвёл 7/512 небезопасных эпизодов: ни одного в первые 100 шагов, медиана первого события — шаг 575. Значит, это проблема поведения в ходе эпизода, а не только стартовой позы.

Slip proxy использует горизонтальную скорость условной нижней точки колеса радиусом 0.07 м при контакте и не корректирует нормаль наклонного рельефа; это не прямая мера физической пробуксовки. Мощность — сумма абсолютных механических `torque × joint velocity`, не электрическое потребление. Логи: `logs/eval_rough_level9_fixedseed_200{1,2,3}.log`, `logs/eval_rough_level9_seed2002_timing.log`.

Stage 2 на том же уровне и seed 2002 дал 507/512 безопасных, включая 98/102 на обратных ступенях, но RMS vx 0.266 против 0.254 у stage 1. Одного seed недостаточно для смены основного кандидата. Лог: `logs/eval_rough_stage2_level9_seed2002.log`.

Stage 3 — отдельная абляция от stage 1: те же PPO, reset ±0.1 рад и 1024 среды, но `max_init_terrain_level=9` вместо 5, ещё 200 обновлений. Сопоставление на одинаковых seeds уровня 9 и flat regression приведены ниже.

Stage 3 завершена за 334.15 с: `logs/rsl_rl/unitree_b2w_rough/2026-09-21_12-37-33_rough_all_levels_stage3/model_548.pt`, SHA-256 `1f7340bd6f2f87cbb6a3afe500fdbafc00221bf40b7dac321b3e8361ddacaaf4`; все веса конечны. Лог обучения `logs/train_b2w_rough_all_levels_stage3.log`. После 200 обновлений средний curriculum terrain level был 5.67.

| Seed level 9 | Stage 1 безопасно | Stage 3 безопасно | Stage 1 обратные ступени | Stage 3 обратные ступени | Stage 3 RMS vx/vy/yaw |
|---|---:|---:|---:|---:|---|
| 2001 | 506/512 | 504/512 | 98/102 | 97/102 | 0.238/0.230/0.194 |
| 2002 | 505/512 | 505/512 | 96/102 | 99/102 | 0.253/0.233/0.203 |
| 2003 | 504/512 | 506/512 | 97/102 | 99/102 | 0.238/0.236/0.193 |

Общий результат stage 3 тот же: 1515/1536 безопасных. Обратные ступени улучшились с 15 до 11 небезопасных эпизодов, но на обычных ступенях стало 6 вместо 2, на boxes 3 вместо 4, на random rough 1 вместо 0. На каждом из трёх seeds каждое семейство набрало не менее 95% по точечной оценке; худшее — 97/102 (95.1%) на обратных ступенях seed 2001. Средний RMS по трём seeds stage 3: vx 0.243, vy 0.233, yaw 0.197, примерно тот же, что stage 1. Slip proxy 0.278/0.304/0.296 м/с, torque saturation 1.34/1.35/1.32%; существенного выигрыша в этих метриках нет. Логи `logs/eval_rough_stage3_level9_seed200{1,2,3}.log`.

Flat regression stage 3 на seed 1001: 128/128 безопасно; RMS `(0.1626, 0.1716, 0.1446)` против flat parent `(0.1633, 0.1702, 0.1457)`, то есть `(-0.42%, +0.85%, -0.76%)`. Лог `logs/eval_flat_regression_stage3_seed1001.log`. Stage 3 — кандидат для дальнейшего сравнения по семействам, но суммарную надёжность stage 1 не превзошла. Независимые rough training seeds приведены ниже; проверка на иных геометриях ещё не выполнена.

### Независимые rough training seeds

Повторили stage 1 с тем же проверенным flat actor seed 54, новым critic/optimizer и независимыми rough RNG seeds 55 и 56; 1024 среды, 350 обновлений, первые 50 только critic. Это независимые *rough* запуски, но у них общий flat родитель. Seed 55 завершён за 622.72 с, checkpoint `logs/rsl_rl/unitree_b2w_rough/2026-09-21_13-56-09_rough_from_flat54_seed55_stage1/model_349.pt`, SHA-256 `765fe2a4cd1438ca3e81c387be570473c5dd3f9db656c4f064ec7a903dd802ed`. Seed 56 завершён за 614.91 с, `logs/rsl_rl/unitree_b2w_rough/2026-09-21_14-11-55_rough_from_flat54_seed56_stage1/model_349.pt`, SHA-256 `64a9646efc52285934170efc623753550f497c34f4e1ab8536a9403c144f1465`. Оба checkpoint содержат конечные веса и итерацию 349. Логи обучения `logs/train_b2w_rough_from_flat54_seed{55,56}_stage1.log`.

Каждый checkpoint проверен на одинаковых mesh seeds 2001–2003, level 9, по 512 сред × 20 с на seed, reset ±0.3 рад. Показаны небезопасные эпизоды, то есть меньше — лучше.

| Rough training seed | Mesh 2001 | Mesh 2002 | Mesh 2003 | Всего / 1536 | Обратные ступени / 306 | Средний RMS vx/vy/yaw |
|---|---:|---:|---:|---:|---:|---|
| 54 | 6 | 7 | 8 | 21 | 15 | 0.242/0.233/0.198 |
| 55 | 6 | 4 | 3 | 13 | 6 | 0.250/0.233/0.193 |
| 56 | 4 | 2 | 6 | 12 | 7 | 0.248/0.233/0.190 |

Ни один из runs 55/56 на этих seeds не опустился ниже 95% безопасности в отдельном семействе. Но 3 mesh seeds одной реализации генератора не задают доверительный интервал для реальных лестниц. Логи `logs/eval_rough_seed{55,56}_level9_seed200{1,2,3}.log`.

Flat regression на seed 1001: оба новых rough checkpoint дали 128/128 безопасных эпизодов. RMS seed 55 `(0.1713, 0.1758, 0.1487)`, seed 56 `(0.1685, 0.1727, 0.1442)`, flat parent `(0.1633, 0.1702, 0.1457)`; ухудшение не превышает проектный допуск 10%. Логи `logs/eval_flat_regression_seed{55,56}_seed1001.log`.

Контрольный rough run с нуля, seed 54, был запущен с бюджетом 350 обновлений, теми же коэффициентами PPO, reset ±0.1 рад и 1024 средами. У него нет flat actor и critic warmup; кроме того, std действий обучаемый, тогда как в warm start он зафиксирован на 0.1. Эти отличия ограничивают причинную интерпретацию сравнения. По просьбе пользователя run **остановлен на обновлении 331/350**: средний terrain level снизился с ~3.5 в начале до 0.0 к обновлению 328 и оставался там, поэтому продолжение этого варианта признано бесполезным. Есть промежуточный `model_300.pt`, но финального checkpoint и closed-loop оценки нет; результат не считать полноценным equal-budget сравнением. Лог `logs/train_b2w_rough_scratch_seed54_equal_budget.log`.
